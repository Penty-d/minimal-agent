# AI 辅助开发记录

本文档记录开发过程中与 AI（Claude Code）协作的原始对话与 Prompt 记录、开发流程、真实排查问题与工程经验总结。

---

## 一、原始对话与 Prompt 记录

以下为本次开发过程中在终端与 AI 交互的 Prompt 记录 （有简单美化）：

### 阶段 1：项目启动与架构约束
1. “梳理极简 Agent Runtime 的架构需求，不要依赖 LangChain、LlamaIndex 等框架”
2. “按照模块职责分步推进，理清核心架构与数据流，不要直接一口气写太多”
3. “维护代码注释专业利落，不要写无意义的注释”
4. “从零实现是不借助第三方 Agent 框架，基础的 env 环境变量加载器和标准库可以正常使用”
5. “代码改动按功能模块拆分提交，Git Commit Message 遵循 Conventional Commits 英文规范，保持干净的提交历史”

### 阶段 2：协议对齐与模型参数
6. “搜一下最新的 DeepSeek 官方 API 接口协议，确认模型参数与推理模式字段规范”
7. “对齐 DeepSeek V4 运行参数：设置模型为 `deepseek-v4-flash`，温度按官方推荐调整为 1.0，最大上下文窗口配置支持长窗口”

### 阶段 3：工具链可用性与联网搜索接入
8. “看一下当前工具链实现： search 现在完全只是空壳，应该要有真实实现”
10. “看一下 todo 工具的实现：todo数据是否和 Session 绑定”
11. “简单介绍一下tool 注册的机制以及 json schema”

### 阶段 4：会话隔离与终端回显排查
12. “session的设计有问题，不同对话的session应该不同保存，现在agent只有一份session”
13. “举例子：data\sessions 中只有一份 session 文件，我在启动 agent 时他自动读取了这份文件，这是为什么？我新启动 agent 不应该是一个新 session 吗”
14. “在切回之前session的时候应该把旧的session内容显示出来”
15. “为什么回显的结果是省略的？调用工具可以是摘要，但模型输出的内容不能省略”
16. “以后不要随便设置回显limit长度”

### 阶段 5：上下文压缩机制与 ReAct 循环核对
18. “看一下当前 Agent 上下文管理与历史压缩的触发机制与具体实现方案”
19. “确认上下文滑动窗口压缩策略：避免用户每次发言都触发压缩，明确仅在超出 Token 预算截断时才进行懒加载摘要”
20. “看一下增量摘要实现细节，确保压缩历史不会丢失核心上下文信息”
21. “当前loop有没有遵循ReAct规则”

### 阶段 6：跨会话长期记忆重构
23. “设计一下memory，在llm认为需要持久化记忆的时候，调一个save，把memory落盘，然后每次请求时把memory放 System Prompt后面”
24. “简化一下memory的索引，不要chunking，直接硬性限制memory长度即可”

### 阶段 7：可观测性、单元测试与复盘
26. “加一下trace，记录全部session信息”
27. “构建一下pytest测试”
28. “整理一下开发过程中遇到的工程问题与排查过程，按规范格式化输出复盘记录”

---

## 二、遇到的问题与解决

### 1. 回溯 session 历史时 UI 未回显

vibecoding 之后，在使用 /use 命令时，我发现对话历史并未回显，用户对先前对话内容一无所知，即使 session 中已经有详细的对话记录。

解决：添加历史记录回显，并修正 AI 设置的 12 轮对话记录 limit。

### 2. 模型回答在历史回显中被截断

回显时发现 Agent 的回答被截断（只显示前 120 字），切回旧会话看不到完整的模型输出，没法接着聊。

解决：回显不再截断用户提问与模型回答，完整多行显示；只有工具调用给摘要。

### 3. session 记录污染

解决完历史记录回显问题后，重启对话发现 agent 的不同 session 的上下文全部混杂，于是追查文件，发现只有一份 session，是 vibecoding 时设计失误。每次新启动 agent 时还默认保持上次 session 内容。

解决：改为每次打开 agent 都新起一个 session；旧会话保留在磁盘，用 /use 显式切回，--resume 可恢复最近会话。

### 4. search 工具没法真正搜索

search 初版是 mock，按关键词查写死的字典。

解决：改用必应 RSS（format=rss）免 Key 返回真实网页结果，国内网络可用；网络不可用时回退内置演示数据，工具永不崩；后端可注入，便于日后替换为带 Key 的正式搜索 API。

### 5. 用户输入未写入 session 历史

初版只在请求里追加用户消息，没写进 session.history，导致持久化缺 user 轮、追问断档。

解决：runtime 先 history.append(user_msg) 再组装请求。

### 6. Context 截断破坏工具配对

按 token 预算截断时，切割点可能落在 tool 消息上，导致发给 API 的请求以 tool 消息开头，违反"tool 消息必须紧跟产生它的 assistant 消息"的协议。

解决：截断后直接丢掉tool信息。
